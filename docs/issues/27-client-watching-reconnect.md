# [Client] Watching a battle, reconnecting mid-match, the verdicts

Labels: enhancement · Type: Feature

## Reported
> make multiplayer similar to topgen if not yet

## What happens today
Covered by #26's skeleton; this ticket is the edges.

## Change
- Watching: a battle the other seat decided is shown from the local seat's viewpoint with both sides remote (no input, no local AI); the result overlay is the same; Continue resolves the local copy.
- Reconnect: `LobbyClient` keeps the match through a drop and reconnects with backoff; the server's `campaign_start` with `replay` rebuilds the campaign silently (no toasts, no sounds, no timers - the AI rounds are stepped to completion inline) and lands on the campaign page, or on the board if a battle is open.
- Verdicts: `match_end` (win / loss / draw, ELO delta, disputed) ends the match wherever it stands; `desync`, `server_restart` and `peer_disconnected` are worded on the overlay; the peer's drop shows a banner with the grace clock.

## Acceptance criteria
- A client killed mid-campaign and restarted lands back in the same campaign with the same hash (manual check with the driver; the server suite covers the replay itself).
- Suggested commit: `#27 client: watching, reconnecting, verdicts`.

Related: #24, #26.

## Done
- Implemented with #26 (`startOnline` replay, `BattleController::setReplaying`, the overlay's `reasonText`).

---
_Generated by [Claude Code](https://claude.ai/code)_
