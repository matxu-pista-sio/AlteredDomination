# [Client] Saves UI: slots with metadata in AppDataLocation, autosave, load from Home and from the in-game menu

Labels: enhancement · Type: Feature

## Reported
> overall better project design

## What happens today
`LoadGame.qml` parses `homeClass.savesData` (the whole file as a string) with `JSON.parse` on every open and deletes a save by rewriting the string back through `deleteGameSave(QString)` (`homemenu.cpp:56`) — no slots, no metadata, one file in the working directory (#7).

## Change
- `SaveStore` (C++, `QAbstractListModel`): slots under `QStandardPaths::AppDataLocation/saves/`, one `<slot>.json` (core schema #7) + `<slot>.meta.json` (country key, flag, round, income share, ISO date, play time); `save(slot)`, `load(slot)`, `remove(slot)`, `autosave` slot.
- `LoadGame.qml`: cards with flag, country, round, date, share bar; *Load* / *Delete* (confirm); the autosave card first.
- In-game menu (Escape): *Save* opens a slot picker with *New slot*; *Quit to menu* warns about unsaved progress.

## Acceptance criteria
- A campaign saved at round 7 loads at round 7 with the same `stateHash`; deleting a slot removes both files; a corrupt file shows an error card instead of crashing.
- Screenshot of the load page with three slots.
- Suggested commits: `#18 client: SaveStore`, `#18 client: LoadGame page`, `#18 client: in-game menu save/quit`.

Related: #7, #13, #15.

## Done
- Commits: `24cfc17` `SaveStore` (slots under `AppDataLocation/saves`, `<slot>.json` + `<slot>.meta.json`, `AD_SAVE_DIR` override) · `744a5f7` Load game page · `7c4a318` in-game menu (Save to a slot, Ctrl+S quick save, autosave at round end).
- Verified headless: Ctrl+S in round 1 writes `save-1.json` + `save-1.meta.json`; the Load page lists the card; Load restores the campaign (`round`, `currentSlot` and the map state match). The core's save tests pin the `stateHash` round trip.
- Screenshots: `load-slots.png`, `campaign-menu.png`, `campaign-saved.png`.

---
_Generated by [Claude Code](https://claude.ai/code)_
