# [Client] Audio: SoundEffect pool and music player with saved volumes; prune the 100 MB of duplicated legacy sounds

Labels: enhancement · Type: Feature

## Reported
> use better UI setup and effects

## What happens today
- Sounds are played with `QSound` (removed in Qt 6) constructed per unit (`graphics/unitgraphics.cpp:38` creates two `QSound`s per unit on the board) and with QML `Audio` elements from `QtMultimedia 5`.
- `data/sounds/` is 100 MB: three 15–24 MB uncompressed map tracks plus their mp3 copies, `intro.wav` 15 MB, `battle1.wav` 15 MB; the unit sounds are 22 small wavs.

## Change
- `Audio` QML singleton (C++): a pool of `QSoundEffect`s per key (click, hover, capture, unit move/strike/explosion per type), `QMediaPlayer` + `QAudioOutput` for music (menu / campaign / battle tracks, cross-fade), master / effects / music volumes saved in settings, mute.
- Keep the 22 unit wavs and click/hover; convert the three map tracks and the battle track to Ogg Vorbis at 96 kbps (≤ 2 MB each) with the conversion script committed (`scripts/convert_audio.sh`); drop the wav/mp3 duplicates, `intro.wav` and `battle1.wav` (they are the same material as the tracks).
- Provenance of the remaining files is the owner's call (#21); until confirmed the tracks stay in the repo as they were.

## Acceptance criteria
- `assets/audio/` ≤ 12 MB total; every sound reachable from QML by key; volume sliders in Settings apply live.
- Suggested commits: `#17 client: Audio singleton`, `#17 assets: converted tracks + pruned duplicates`, `#17 client: hook sounds into HUD and battle`.

Related: #16, #21.

---
_Generated by [Claude Code](https://claude.ai/code)_
