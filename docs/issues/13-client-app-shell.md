# [Client] App shell: one window with StackView pages (Intro, Home, New game with country picker, Load game, Settings, Codex) over the core bridge

Labels: enhancement · Type: Feature

## Reported
> only use Qml (no qt widgets), this one will only be fore desktop

## What happens today
- `HomeMenu` is a `QQuickView`; the game is a `QWidget` (`MainMenu`, `mainmenu.ui`) hosting a `QGraphicsView` and two `QQuickWidget`s; the city sheet, recruit, move, attack and options panels are five more frameless `QQuickView` windows with `Qt::ToolTip` flags (`graphics/mapview.cpp:60-140`) that each `setEnabled(false)` the map while open. Focus and z-order break on every desktop environment.
- The intro (`scripts/home/Intro.qml`) cannot be skipped; the home screen animates 18 PNG frames of a rotating earth.
- The new-game page is a `ComboBox` of 174 names with no search, flag or info.

## Change
- `client/src/main.cpp`: `QGuiApplication`, high-DPI pass-through, style + fonts, `QQmlApplicationEngine::loadFromModule("AD", "Main")`, `--profile`, `AD_DRIVE` hook (#19).
- `Main.qml`: `ApplicationWindow` 1280×800 (min 1024×640), `StackView` with cut transitions, pages `Intro` (skippable logo reveal with a shader wipe), `Home` (animated map background from the real world data, mode buttons: New game / Load game / Codex / Settings / Quit, version bottom-left), `NewGame` (searchable country grid with flags, GDP, city count; mode GDP/Equality; difficulty; seed; hotseat toggle to add a second human; **Start** flies into the campaign), `LoadGame` (slots with metadata, delete with confirmation), `SettingsView` (theme, audio, effects, controls), `Codex` (rules + the 11 units with pattern diagrams drawn from the catalog).
- Bridge singletons per [docs/ARCHITECTURE.md](https://github.com/matxu-pista-sio/AlteredDomination/blob/claude/dreamy-volta-pvb4kh/docs/ARCHITECTURE.md): `GameController`, `WorldModel`, `LinkModel`, `CountryModel`, `CityUnitsModel`, `CatalogModel`, `SaveStore` (models updated by deltas from core events).

## Acceptance criteria
- Every page reachable from Home and back with the keyboard alone (Tab/Enter/Escape).
- Screenshots of Intro, Home, New game (with a search term typed), Codex unit page in the issue.
- No `QQuickView`, `QWidget` or second window anywhere in the client.
- Suggested commits: `#13 client: main + Main.qml + Intro`, `#13 client: bridge models`, `#13 client: Home`, `#13 client: NewGame`, `#13 client: LoadGame + Settings`, `#13 client: Codex`.

Related: #12, #14, #18.

## Done
- Commits: `24cfc17` bridge models + controllers · `744a5f7` the shell pages · `c93dd3c` campaign/battle routes.
- One `ApplicationWindow` + `StackView`; no `QQuickView`, `QWidget` or second window anywhere (`grep -rn "QQuickView\|QWidget" client/` is empty).
- Keyboard: every page is a `FocusScope`; Tab/Enter/Escape reach every page and back (verified with the driver: `key Escape` from New game lands on Home with a focused button).
- Screenshots: `intro.png`, `home.png`, `newgame.png`, `newgame-search.png` ("ja" typed), `codex.png`, `codex-units.png`, `codex-controls.png`, `load.png`.

---
_Generated by [Claude Code](https://claude.ai/code)_
