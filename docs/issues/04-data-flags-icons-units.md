# [Data] SVG flags (flag-icons), generated SVG unit icons, cleaned units.json with a validator

Labels: enhancement · Type: Feature

## Reported
> use newr svg features [...] make it wow

## What happens today
- Flags are 1.8 MB of PNGs (`data/flags/*.png`, 250 files) scaled with `QImage::scaled` at load (`mapview.cpp:191`); unit art is PNG (`data/units/*.png`, 3.8 MB incl. a `junks/` folder) drawn with `QImage(":/...")` **inside `paint()`** on every repaint (`graphics/unitgraphics.cpp:110-120`).
- `scripts/units.json` spells a class `"machin"`, has no display names or descriptions, and nothing checks it.

## Change
- `assets/flags/<iso2>.svg` from lipis/flag-icons (4×3, MIT), only the countries of `world.json`, fetched by `scripts/fetch_flags.py`; `LICENSE-flag-icons.txt` beside them.
- `scripts/gen_unit_svgs.py`: deterministic 64×64 stroke glyphs for the 11 unit types using gradients and a soft-glow filter (Qt SVG ≥ 6.7 renders filters; `VectorImage` draws them on the scene graph) → `assets/units/icons/<key>.svg` + `assets/units/preview.html`.
- `assets/units/units.json` curated: `key`, `name`, `description`, `cost`, `classes` (`human|machine` × `land|air`), `moves[] {dx, dy, path[]}`, `strikes[] {dx, dy, path[], affects[]}` — same patterns as today, `"machin"` → `"machine"`.
- `scripts/check_units.py` validates the schema ([docs/DATA_PIPELINE.md](https://github.com/matxu-pista-sio/AlteredDomination/blob/claude/dreamy-volta-pvb4kh/docs/DATA_PIPELINE.md) §7); CI runs it.

## Acceptance criteria
- One flag per country in `world.json`, all SVG, total ≤ 1.5 MB.
- 11 icons, no `<text>`, no external references; the preview page shows them on a dark and a light disc.
- `check_units.py` green; the core catalog tests of #5 load the file.
- Suggested commits: `#4 assets: flags`, `#4 assets: unit icon generator + icons`, `#4 assets: units.json + validator`.

Related: #2, #5, #16.

---
_Generated by [Claude Code](https://claude.ai/code)_
