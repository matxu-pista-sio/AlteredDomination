# [Core] Wire codec: JSON round trips for the campaign and board commands

Labels: enhancement · Type: Task

## Reported
> make multiplayer similar to topgen if not yet

## What happens today
`Command` and `BattleCommand` are C++ variants with no serialised form; the save format writes state, never commands.

## Change
`core/include/ad/core/netcodec.hpp`: `commandToJson` / `commandFromJson`, `battleCommandToJson` / `battleCommandFromJson`, `battleOutcomeToJson` / `battleOutcomeFromJson`, `commandKind`. Compact objects with a `kind` (PROTOCOL.md §5); the decoders refuse malformed or out-of-range documents with a reason. No Qt.

## Acceptance criteria
- Every command kind round-trips bit for bit; a refused document names the field.
- Suggested commit: `#23 core: wire codec for campaign and battle commands`.

Related: #22, #24, #26.

## Done
- Commit `369a762`; `core/tests/test_netcodec.cpp` (5 cases, 72 assertions).

---
_Generated by [Claude Code](https://claude.ai/code)_
