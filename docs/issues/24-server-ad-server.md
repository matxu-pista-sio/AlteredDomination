# [Server] ad-server: accounts, ELO, the ranked queue, the setup negotiation and the lockstep relay

Labels: enhancement · Type: Feature

## Reported
> make multiplayer similar to topgen if not yet

## What happens today
No server exists in the repository.

## Change
`server/` (Qt Core + WebSockets + Sql, links `ad::core` for the world), mirroring TopGen's `topgen-server` where the protocol agrees (docs/PROTOCOL.md):
- `Storage`: SQLite `players` (token identity, `#N` name de-collision, ELO K=32 in one transaction) and `matches`; open matches are abandoned at startup.
- `Matchmaker`: one ranked queue, paired in batches every 7 s, neighbours on the ladder.
- `Server`: sessions (hello, displacement, 15 s auth deadline, 512 cap), stats and the leaderboard, pairing with the world-hash check, the two-stage setup with clocks, the numbered relay with the in-memory log, `sync` and `desync`, two-report results, server-ruled resignations, 60 s reconnect grace with the log replay, forfeits, the command-flood ban.
- `main`: `--port --db --world --verbose`.
- `server/tests`: doctest over real `QWebSocket`s (matchmaker, storage, 21 server scenarios).
- Root CMake `AD_BUILD_SERVER`, preset `server-release`; CI builds and tests it.

## Acceptance criteria
- The suite passes; a `server-release` build runs headless with only Core/WebSockets/Sql present.
- Suggested commits: `#24 server: storage and the ranked matchmaker`, `#24 server: the WebSocket server`.

Related: #22, #23, #28.

## Done
- Commits `8ecd865`, `6e8c882`; 31 test cases / 404 assertions; `server/README.md`.

---
_Generated by [Claude Code](https://claude.ai/code)_
