# [Client] The lobby: LobbyClient, the Online page, the shared country picker

Labels: enhancement · Type: Feature

## Reported
> make multiplayer similar to topgen if not yet

## What happens today
The home menu has no online entry.

## Change
- `client/src/net/lobbyclient.{h,cpp}`: a QML singleton over `QWebSocket` - the hello with the token kept per server in `QSettings` (`DEV_SERVER_ADDRESS` overrides the address for a run), reconnect with backoff, stats and the leaderboard, the queue, the setup frames with a countdown, and the relay frames handed to `GameController`.
- `Lobby.qml`: connection, record, queue / setup panel, the ladder; `CountryPicker.qml` extracted from New game and reused for the banner stage.
- Home "Online", `Main.qml` routes (`page lobby` for the driver).

## Acceptance criteria
- Screenshots: the lobby offline, online with the ladder, paired (terms), the banner stage.
- Suggested commit: `#25 client: the lobby - LobbyClient, Lobby page, CountryPicker`.

Related: #22, #24, #26.

## Done
- See the commit of that name; screenshots `docs/screenshots/net-lobby.png`, `net-lobby-paired.png`, `net-lobby-countries.png`.

---
_Generated by [Claude Code](https://claude.ai/code)_
