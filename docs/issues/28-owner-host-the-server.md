# [Owner] Host ad-server: a machine, TLS, and the client's default address

Labels: documentation · Type: Task · Assignee: matxu-pista-sio

## Reported
> make multiplayer similar to topgen if not yet

## What happens today
The server builds and passes its suite, and the client dials `ws://localhost:9977` by default (`LobbyClient::kDefaultServerUrl`, editable in the lobby). Nobody is running it.

## Decision needed (owner only)
1. Where it runs: a small VPS (one process, one SQLite file, ~50 MB) - the same box as TopGen's server would do.
2. TLS: put it behind a reverse proxy (nginx / caddy) that terminates `wss://` and forwards to the loopback port (server/README.md).
3. The default address: set `kDefaultServerUrl` to the public `wss://` URL and commit, or leave players to type it.

## Acceptance criteria
- A decision recorded here; the default address updated if chosen.

---
_Generated by [Claude Code](https://claude.ai/code)_
