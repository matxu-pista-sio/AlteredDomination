# [Owner] Asset provenance: confirm or replace the legacy sounds, fonts and logo

Labels: documentation · Type: Task · Assignee: matxu-pista-sio

## Reported
> mark issues that require my action only if necessary

## What happens today
- `data/sounds/` (100 MB): music tracks `map1/2/3`, `ambiant1`, `battleambiant`, `intro`, `battle1` and 22 unit effects — no source or license recorded.
- `data/fonts/`: `Coalition_v2..ttf`, `DESTROY_.ttf`, `District.ttf` — no license files; the modernization replaces them with bundled OFL fonts (Rajdhani, Source Sans 3), so nothing depends on them any more.
- `data/logo/adlogo.svg` embeds a 350 KB PNG (not a vector); `adicon.svg` is 275 KB for the same reason; `adlogo.bmp` is 2.3 MB.

## Decision needed (owner only)
1. Sounds: confirm the tracks and effects are yours to publish (or name their source/license) — until then #17 keeps them as they are but converted; say the word and the tracks are replaced by generated ambience.
2. Logo: provide the vector source of the AD mark if you have one; otherwise the modernization traces a clean SVG from `adlogo.svg`'s bitmap and keeps its look (you may want to veto that).

## Acceptance criteria
- `THIRD_PARTY.md` names a license or source for every audio file and the logo.

Related: #17, #20.

---
_Generated by [Claude Code](https://claude.ai/code)_
